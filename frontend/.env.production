# Production pin for the Westphalia frontend (GenLayer Studio Next, chainId 61997).
#
# Next.js loads this file for production builds. Every value is public
# (NEXT_PUBLIC_* is inlined into the client bundle) -- never put a secret here.
# lib/networks.ts already falls back to the same values, so this file only makes
# the production target explicit; it does not add any new secret surface.
NEXT_PUBLIC_DIPLOMATIC_CONTRACT_ADDRESS=0xa92Ea76aeB17BBE7bc88De74E53D5cCB4d30eBED
NEXT_PUBLIC_GENLAYER_RPC_URL=https://studio-next.genlayer.com/api
